# یافته‌های تحقیق برای ارتقای Landing Page

منبع رسمی بررسی‌شده: [README سیستم قالب Bazino GameNet](https://github.com/paymanshafayan/bazino-gamenet-portal/blob/main/src/themes/README.md).

راهنما مشخص می‌کند که بسته‌ی نصب‌پذیر باید سه فایل اصلی `theme.json`، `theme.css` و `theme.js` داشته باشد. کامپوننت Home در `theme.js` باید داده‌محور باشد و از props استاندارد SDK استفاده کند.

چهار سطح محتوایی اجباری صفحه اصلی عبارت‌اند از:

| بخش اجباری | prop استاندارد | تصمیم ارتقا |
| --- | --- | --- |
| ژانرهای بازی | `gameGenres` | تبدیل به کارت‌های تصویری با فیلتر و نشانگر دسته‌بندی |
| تورنمنت‌های فعال | `tournaments` | فیلتر دسته‌بندی بازی و وضعیت رسمی رویداد |
| نتایج مسابقات | `matchHistory` | اسکوربورد فقط با داده‌ی تأییدشده، بدون امتیاز ساختگی |
| سالن‌ها و خدمات | `loungeSections` | اسلایدر تعاملی VIP/Café با کنترل صفحه‌کلیدی |

راهنما بخش‌های Hero/اسلایدر، تعرفه‌ها، CTA، درباره، محصولات و تماس را اختیاری می‌داند. در قالب بازینو، این سطوح برای ساخت هفت فصل سینمایی استفاده می‌شوند: Hero، Console Arena، Tournaments، Match History، VIP/Café، Entry Signal و Visit/Mobile Signal.

اصول اجرایی استخراج‌شده شامل استفاده از داده‌های رسمی به‌جای ادعای ساختگی، حفظ سه فایل اجباری پکیج، پوشش تمام صفحات با CSS مشترک، و حفظ دسترسی‌پذیری و واکنش‌گرایی است. ارتقای جدید باید از افکت‌های hover و scroll-reveal با زمان کوتاه و قابل کاهش در `prefers-reduced-motion` استفاده کند.

## محدودیت‌های اجرایی

لینک‌های واقعی فروشگاه Android و iOS در داده‌های فعلی پروژه مشاهده نشدند؛ بنابراین دکمه‌ها باید با مقصد قابل‌تنظیم یا وضعیت «به‌زودی» ساخته شوند و نباید URL جعلی تولید شود. تصاویر کارت‌های بازی باید original و بدون لوگو یا شخصیت دارای مالکیت فکری باشند. تصاویر Mona جزو این بسته‌ی تولیدی نیستند و هویت قفل‌شده‌ی او نباید تغییر کند.
