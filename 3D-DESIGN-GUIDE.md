# راهنمای طراحی سه‌بعدی تعاملی بازینو

> **تاریخ تدوین:** ۲۰۲۶-۰۹-۱۱  
> **مرجع مشاهده‌شده:** `https://shopify.design/`  
> **روش بررسی:** اتصال واقعی CDP به Chrome کارفرما، مشاهده با Vision، حرکت ماوس، کلیک، Drag و اسکرول  
> **هدف:** تعریف دقیق استاندارد «طراحی سه‌بعدی» مورد انتظار کارفرما برای تجربه‌های آیندهٔ Bazino

---

## ۱) تعریف طراحی سه‌بعدی مورد انتظار

در این پروژه، «طراحی سه‌بعدی» به معنی استفاده از یک تصویر رندرشده، چند سایه، گرادیان، کارت مایل یا `transform: perspective()` تزئینی نیست. طراحی سه‌بعدی مطلوب یک **صحنهٔ زنده، سینمایی و تعاملی** است که ورودی کاربر بخشی از کارگردانی آن باشد.

تجربه باید دست‌کم سه ویژگی محوری داشته باشد:

1. **عمق واقعی و قابل‌درک:** دوربین، پرسپکتیو، اختلاف سرعت لایه‌ها، انسداد اجزا، مقیاس وابسته به عمق و نور/فضای حجمی.
2. **واکنش مستقیم به کاربر:** حرکت ماوس، کلیک، Drag و اسکرول باید پاسخ بصری نرم و قابل‌مشاهده ایجاد کنند.
3. **حرکت هدفمند:** اجزا فقط تکان نخورند؛ باید وارد صحنه شوند، مسیر داشته باشند، در جای درست settle شوند و ترکیب نهایی صفحه را بسازند.

اصل کلیدی:

> صفحه یک پوستر ثابت با افکت نیست؛ یک فضای کارگردانی‌شده است که کاربر با حرکت خود آن را اجرا می‌کند.

---

## ۲) آنچه واقعاً در مرجع مشاهده و اثبات شد

### ۲-۱) حالت اولیه

صفحه در حالت اولیه دارای یک ترکیب بسیار مینیمال و سفید بود:

- لوگوی کوچک در بالا؛
- فضای خالی وسیع برای ایجاد تعلیق؛
- CTA کپسولی در هدر؛
- یک کپسول سه‌بعدی کوچک و رنگی در پایین راست؛
- محتوای اصلی که در ابتدا پنهان یا بسیار کم‌رنگ بود.

این سکوت بصری باعث می‌شود فعال‌شدن صحنهٔ سه‌بعدی اثر نمایشی بیشتری داشته باشد.

### ۲-۲) کلیک روی کپسول

با کلیک واقعی CDP روی کپسول پایین راست در حدود مختصات `1286×576`، نمای سفید به یک Hero سه‌بعدی سیاه تبدیل شد. این تغییر صرفاً navigation نبود؛ URL همان `https://shopify.design/` باقی ماند و وضعیت بصری داخل همان صفحه تغییر کرد.

صحنهٔ بازشده شامل موارد زیر بود:

- یک فضای تونل‌مانند سیاه؛
- شبکه‌های Wireframe سفید روی کف، دیواره و عمق؛
- خطوط پرسپکتیو و نقاط نورانی؛
- سطوح منحنی و ساختارهای حجمی پیرامونی؛
- حروف بسیار بزرگ و سه‌بعدی عبارت `Make the new normal`؛
- کپسول رنگی شناور که همچنان بخشی از تجربهٔ تعاملی باقی ماند؛
- یک عنصر واقعی `canvas` در DOM.

### ۲-۳) واکنش واقعی به حرکت ماوس

پس از بازشدن Hero، ماوس در سه موقعیت چپ، مرکز و راست حرکت داده شد و پس از هر حرکت تصویر مستقلی ثبت شد. تفاوت تصاویر، واکنش صحنه را اثبات کرد:

- زاویهٔ دید دوربین تغییر کرد؛
- حروف در محور عمق نسبت به یکدیگر جابه‌جا شدند؛
- اندازه و انسداد حروف تغییر کرد؛
- بخش‌های متفاوتی از شبکهٔ فضایی نمایان شد؛
- خطوط و سطوح پیرامونی با تغییر دید، پرسپکتیو متفاوتی پیدا کردند؛
- پاسخ صحنه نرم و پیوسته بود، نه یک تعویض دوحالتهٔ ساده.

شواهد ثبت‌شده در runtime همان جلسه:

```text
shots/live/capsule-opened.jpg
shots/live/capsule-left.jpg
shots/live/capsule-center.jpg
shots/live/capsule-right.jpg
```

> این فایل‌ها شواهد runtime هستند و طبق قواعد امنیت/حجم نباید الزاماً وارد Git شوند. ادعای واکنش Hero بر مشاهدهٔ واقعی همین چهار تصویر استوار است.

### ۲-۴) کلیک و Drag

کلیک و Drag افقی واقعی نیز از طریق `Input.dispatchMouseEvent` اجرا شد. صفحه crash یا navigate نشد و صحنه کنترل خود را حفظ کرد. بااین‌حال اثر مستقل Drag در یک توالی فریم‌به‌فریم کنترل‌شده به اندازهٔ واکنش Mouse Move مستند نشد؛ بنابراین در این سند ادعای چرخش مشخص ناشی از Drag مطرح نمی‌شود.

### ۲-۵) اسکرول و گالری

در ادامهٔ صفحه این ویژگی‌ها دیده شد:

- شبکهٔ سه‌ستونه با ریتم Masonry؛
- کارت‌های بزرگ با شعاع گوشه زیاد؛
- محتوای تصویری و سه‌بعدی متنوع داخل کارت‌ها؛
- ترکیب آبجکت، UI، متن و محصول در چند سطح عمق؛
- کپسول شناور که همراه viewport باقی می‌ماند؛
- ادامهٔ بسیار بلند صفحه با ارتفاع اندازه‌گیری‌شدهٔ حدود `11163px` در viewport `1366×641`.

اسکرول واقعی تا حدود `1635px` انجام شد. تغییر محتوای قابل‌مشاهده و رسیدن اجزا به ترکیب‌های جدید تأیید شد، اما حرکت «قبل/حین/بعد» همهٔ انیمیشن‌های ورود به‌صورت فریم‌به‌فریم ثبت نشد. بنابراین:

- وجود تجربهٔ اسکرول پویا و reveal تدریجی: **مشاهده‌شده**؛
- جزئیات دقیق easing، مدت، offset و ترتیب تمام ورودها: **هنوز اندازه‌گیری‌نشده**.

---

## ۳) اجزای سازندهٔ زبان بصری

### ۳-۱) فضای سه‌بعدی

صحنه باید دارای دستگاه فضایی منسجم باشد:

- دوربین با میدان دید کنترل‌شده؛
- نقاط نزدیک با حرکت بیشتر و نقاط دور با حرکت کمتر؛
- مقیاس و وضوح وابسته به عمق؛
- سطوح مرجع مانند Grid، تونل، کف یا حلقه‌های فضایی؛
- انسداد واقعی یا شبیه‌سازی‌شده بین لایه‌ها؛
- نورها و خطوطی که عمق را توضیح دهند، نه صرفاً تزئین کنند.

### ۳-۲) تایپوگرافی به‌عنوان آبجکت

متن Hero باید بتواند نقش هندسهٔ اصلی صحنه را بگیرد:

- حروف Extrude یا چندلایه؛
- Outline روشن روی حجم تاریک؛
- توزیع کلمات در چند صفحهٔ عمق؛
- عبور دوربین یا تغییر زاویه نسبت به حروف؛
- حفظ خوانایی در لحظهٔ settle، حتی اگر هنگام حرکت بخشی از متن قطع یا پوشانده شود.

### ۳-۳) نشانگر تعاملی

کپسول مرجع فقط cursor سفارشی نیست؛ یک دعوت بصری به تعامل است:

- فرم ساده و قابل‌شناسایی؛
- رنگ متغیر برای نشان‌دادن وضعیت؛
- حرکت با lag اندک و spring؛
- امکان تبدیل‌شدن به trigger یا راهنمای کلیک؛
- کنتراست کافی در حالت روشن و تاریک؛
- عدم مسدودکردن تعامل عناصر زیرین.

### ۳-۴) شبکهٔ کارت‌ها

پس از Hero، صفحه نباید به یک لیست تخت تبدیل شود. کارت‌ها باید ادامهٔ همان جهان حرکتی باشند:

- اندازه‌ها و نسبت‌های متنوع؛
- ریتم سه‌ستونه یا ترکیب نامتقارن کنترل‌شده؛
- محتوای دارای عمق داخلی؛
- Hoverهای متناسب با محتوا، نه یک افکت واحد برای همه؛
- Reveal مرحله‌ای بر اساس ورود به viewport؛
- فاصلهٔ سفید کافی تا صحنه‌ها فرصت تنفس داشته باشند.

---

## ۴) مدل تعامل مورد انتظار

### ۴-۱) حرکت ماوس

حرکت ماوس باید به ورودی نرمال‌شده تبدیل شود:

```text
mouseX: -1 … +1
mouseY: -1 … +1
```

این ورودی می‌تواند هم‌زمان روی موارد زیر اثر بگذارد:

- yaw و pitch دوربین؛
- اختلاف مکان لایه‌های foreground/background؛
- چرخش محدود حروف یا آبجکت‌ها؛
- جهت نور یا highlight؛
- موقعیت ذرات و خطوط؛
- شکل یا رنگ نشانگر کپسولی.

حرکت نباید یک‌به‌یک و خشک باشد. مقدار هدف باید با damping، lerp یا spring دنبال شود تا صحنه وزن داشته باشد.

### ۴-۲) کلیک

کلیک باید یک تغییر معنادار ایجاد کند، برای مثال:

- بازکردن یا ورود به جهان سه‌بعدی؛
- تغییر حالت صحنه؛
- نزدیک‌شدن دوربین؛
- بازشدن یک لایهٔ محتوایی؛
- تغییر وضعیت کپسول و سپس اجرای transition.

بازخورد کلیک باید فوری آغاز شود، حتی اگر transition کامل چند ثانیه طول بکشد.

### ۴-۳) Drag

اگر Drag پشتیبانی می‌شود:

- شروع Drag باید از کلیک معمولی قابل‌تفکیک باشد؛
- دوربین یا آبجکت باید محدودیت زاویه و inertia داشته باشد؛
- پس از رهاکردن، حرکت با damping متوقف یا به نقطهٔ تعادل بازگردد؛
- Drag نباید انتخاب متن یا اسکرول ناخواسته ایجاد کند؛
- در موبایل باید معادل touch داشته باشد و با اسکرول عمودی تعارض نکند.

### ۴-۴) اسکرول

اسکرول باید timeline تجربه باشد، نه فقط جابه‌جایی سند:

- عنصر پیش از ورود، وضعیت اولیهٔ مشخص داشته باشد؛
- هنگام نزدیک‌شدن به viewport، حرکت آغاز شود؛
- چند جزء با stagger کنترل‌شده وارد شوند؛
- حرکت به settle واقعی ختم شود؛
- اختلاف سرعت لایه‌ها عمق ایجاد کند؛
- در صورت اسکرول معکوس، رفتار reverse یا reset از قبل تعریف شود؛
- از انیمیشن‌های بیش‌ازحد و هم‌زمان که تمرکز را نابود می‌کنند پرهیز شود.

الگوی مطلوب ورود:

```text
خارج قاب / کوچک / محو / چرخیده
                 ↓
حرکت با easing و stagger
                 ↓
کاهش سرعت نزدیک مقصد
                 ↓
settle در ترکیب نهایی بدون پرش
```

---

## ۵) اصول Motion Design

### ۵-۱) حرکت باید وزن داشته باشد

- عناصر بزرگ کندتر و سنگین‌تر؛
- ذرات و نشانه‌های کوچک سریع‌تر؛
- overshoot محدود و هدفمند؛
- توقف‌ها نرم و بدون snap ناخواسته؛
- تغییر جهت بدون شکستن پیوستگی سرعت.

### ۵-۲) سلسله‌مراتب حرکت

در هر لحظه یک حرکت اصلی وجود داشته باشد:

1. حرکت اصلی: تغییر دوربین یا ورود Hero؛
2. حرکت ثانویه: حروف و لایه‌ها؛
3. حرکت محیطی: ذرات، خطوط و نور؛
4. micro-interaction: کپسول، CTA و hover.

اگر همه‌چیز با شدت یکسان حرکت کند، عمق و تمرکز از بین می‌رود.

### ۵-۳) پیوستگی میان حالت‌ها

حالت سفید، فعال‌شدن صحنهٔ سیاه و ورود به محتوای صفحه باید یک روایت پیوسته بسازند. Crossfade ساده فقط زمانی کافی است که با تغییر مقیاس، blur، عمق یا حرکت دوربین همراه باشد.

### ۵-۴) عملکرد

کیفیت سه‌بعدی بدون عملکرد روان پذیرفته نیست:

- هدف دسکتاپ: نزدیک به `60fps`؛
- استفاده از `requestAnimationFrame` فقط هنگام نیاز؛
- توقف یا کاهش رندر در تب پس‌زمینه؛
- محدودکردن DPR؛
- lazy-load صحنه‌ها و textureها؛
- استفاده از transform/opacity برای حرکت DOM؛
- پرهیز از layout thrashing؛
- fallback مناسب برای سخت‌افزار ضعیف.

---

## ۶) کاربرد برای Bazino

### ۶-۱) Hero پیشنهادی

Hero بازینو باید یک صحنهٔ گیمینگ زنده باشد، نه ویدئوی خام. مطابق قانون پروژه، Hero می‌تواند از **frame-motion tour** و لایه‌های تعاملی استفاده کند:

- حرکت دوربین در سالن یا جهان بازینو بر اساس pointer؛
- لوگو/نام Bazino به‌صورت هندسهٔ فضایی؛
- نورهای نئون با parallax؛
- CTA یا کپسول تعاملی برای ورود به تجربه؛
- کارت‌های رویداد، مسابقه و رزرو در عمق‌های متفاوت؛
- transition پیوسته از Hero به بخش‌های بعدی.

استفاده از `<video>` خام به‌عنوان Hero مجاز نیست. اگر frame sequence استفاده شود، حرکت باید با اسکرول/ماوس کنترل و با لایه‌های UI واقعی ترکیب شود.

### ۶-۲) ورود بخش‌ها هنگام اسکرول

برای بخش‌های بازینو:

- کارت‌های Games از عمق/طرفین وارد و در Grid قفل شوند؛
- براکت مسابقات مرحله‌ای رسم شود و خطوط پس از کارت‌ها ظاهر شوند؛
- لیدربورد با stagger رتبه‌ها assemble شود؛
- تصاویر Gallery با depth و clip reveal وارد شوند؛
- Prices و Packages مانند پنل‌های فضایی settle شوند؛
- عناصر ثابت مانند Header و CTA از حرکت محتوایی جدا بمانند.

### ۶-۳) هویت بصری

مرجع برای منطق تعامل است، نه برای کپی هویت Shopify. اجرای بازینو باید هویت مستقل خود را حفظ کند:

- پالت تاریک، نئون و طلایی/فیروزه‌ای بازینو؛
- فضای گیمینگ و مسابقات؛
- تایپوگرافی و محتوای اختصاصی؛
- Mona و دارایی‌های خود پروژه در صورت نیاز؛
- صفر درخواست خارجی در بستهٔ نهایی قالب؛
- داده‌ها از props پورتال، بدون hardcode غیرمجاز.

---

## ۷) دسترس‌پذیری و حالت‌های جایگزین

- رعایت `prefers-reduced-motion`؛
- امکان استفاده از CTA و محتوا بدون حرکت ماوس؛
- focus قابل‌مشاهده برای کنترل‌های کلیکی؛
- عدم وابستگی پیام اصلی به Canvas؛
- fallback تصویری یا سبک برای WebGL ناموجود؛
- جلوگیری از حرکت شدید دوربین و motion sickness؛
- حفظ کنتراست و خوانایی متن در همهٔ زوایا؛
- عدم قفل‌کردن اسکرول اصلی بدون ضرورت.

---

## ۸) معیارهای پذیرش طراحی سه‌بعدی

یک صفحه فقط زمانی «سه‌بعدی تعاملی» پذیرفته می‌شود که موارد زیر واقعاً آزموده شوند:

### ساختار و ظاهر

- [ ] عمق، پرسپکتیو و لایه‌بندی در تصویر ثابت قابل‌تشخیص است.
- [ ] صحنه هویت بازینو دارد و کپی مستقیم مرجع نیست.
- [ ] متن و CTA در حالت settle خوانا هستند.
- [ ] موبایل و دسکتاپ ترکیب‌بندی مستقل و سالم دارند.

### تعامل

- [ ] Mouse Move حداقل دو تغییر بصری قابل‌اندازه‌گیری ایجاد می‌کند.
- [ ] Click بازخورد فوری و تغییر حالت معنادار دارد.
- [ ] Drag، اگر اعلام شده، با فریم‌های قبل/حین/بعد اثبات می‌شود.
- [ ] Touch جایگزین قابل‌استفاده دارد.

### اسکرول

- [ ] هر بخش وضعیت اولیه، transition و settle مشخص دارد.
- [ ] ورود عناصر با اسکرول در چند فریم متوالی ثبت می‌شود.
- [ ] اسکرول سریع باعث پرش یا گم‌شدن عناصر نمی‌شود.
- [ ] اسکرول معکوس رفتار تعریف‌شده دارد.

### عملکرد و پایداری

- [ ] کنسول بدون خطای runtime است.
- [ ] صحنه در تب پس‌زمینه منابع غیرضروری مصرف نمی‌کند.
- [ ] در viewportهای هدف overflow ناخواسته وجود ندارد.
- [ ] حالت Reduced Motion و fallback تست شده‌اند.

### شواهد اجباری

برای هر ادعای نهایی باید این شواهد وجود داشته باشد:

1. تصویر حالت اولیه؛
2. تصویر پس از کلیک؛
3. حداقل سه تصویر با ماوس در چپ/مرکز/راست؛
4. توالی قبل/حین/بعد یک Drag؛
5. توالی چندمرحله‌ای اسکرول برای هر reveal مهم؛
6. گزارش console و اندازهٔ viewport؛
7. تفکیک روشن «مشاهده‌شده»، «اندازه‌گیری‌شده» و «تست‌نشده».

---

## ۹) ضدالگوها

موارد زیر به‌تنهایی طراحی سه‌بعدی محسوب نمی‌شوند:

- کارت با `box-shadow` و hover چند درجه‌ای؛
- تصویر PNG سه‌بعدی بدون تعامل؛
- ویدئوی پس‌زمینهٔ خام؛
- parallax یک‌لایه؛
- انیمیشن loop بی‌ارتباط با کاربر؛
- حرکت همهٔ اجزا با یک easing و سرعت؛
- استفادهٔ نمایشی از WebGL بدون نقش در تجربه؛
- اسکرول‌جکینگ سنگین که کنترل کاربر را می‌گیرد؛
- ادعای واکنش‌پذیری بدون تصاویر یا اندازه‌گیری قبل/بعد.

---

## ۱۰) نتیجهٔ راهبردی

معیار طراحی سه‌بعدی بازینو از این پس باید این باشد:

> یک تجربهٔ فضایی، سینمایی و قابل‌کنترل که در آن دوربین، تایپوگرافی، آبجکت‌ها، نور و UI با حرکت ماوس، کلیک، Drag و اسکرول پاسخ هماهنگ می‌دهند؛ عناصر با انیمیشن هدفمند وارد می‌شوند و با وزن و easing مناسب در ترکیب نهایی قرار می‌گیرند.

این سند مرجع طراحی و QA است. هر پیاده‌سازی آینده باید علاوه بر ظاهر، با ورودی واقعی مرورگر و شواهد تصویری چندمرحله‌ای ارزیابی شود.
