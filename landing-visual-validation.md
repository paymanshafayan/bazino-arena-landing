# اعتبارسنجی بصری Landing Page

## دسکتاپ

صفحه‌ی اصلی در عرض ۱۲۸۰ پیکسل با هفت فصل قابل‌تشخیص نمایش داده شد: Hero، Console Arena، Active Tournaments، Match History، Lounge Services، Entry Signal و Visit/Mobile Signal. Hero همچنان از ویدئوی تأییدشده‌ی Mona بدون پنل Replay/Final CTA استفاده می‌کند. پس از اصلاح بصری، بخش پاس‌ها از سطح زرد تخت به زمینه‌ی سرمه‌ای سینمایی با قاب آبی تبدیل شد؛ لایه‌های آبی، کارت‌های گوشه‌برش‌خورده و اسکوربورد در فصل‌ها تکرار می‌شوند.

## موبایل

در عرض ۳۹۰ پیکسل، بخش‌ها به‌صورت عمودی و خوانا ترتیب داده شدند. کارت‌های تجربه، اسکوربورد دارای اسکرول افقی کنترل‌شده، سرویس‌های لانژ، پاس‌های ورود و کارت اپلیکیشن بدون خروج از قاب نمایش داده می‌شوند. Hero بدون کنترل‌های پایانی باقی می‌ماند و CTA اصلی رزرو در متن Hero در دسترس است.

## نکته

پوستر ویدئوی Mona به‌صورت مسیر ذخیره‌سازی پایدار در زمان بیلد resolve نمی‌شود و طبق رفتار مورد انتظار در runtime resolve خواهد شد؛ بیلد موفق است. هشدار chunk-size موجود است و خطای TypeScript یا LSP گزارش نشده است.
