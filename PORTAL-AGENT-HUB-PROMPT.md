# پرامپت ارتقاء پورتال به قالب هاب (BAZINO HUB THEME PROMPT)

> **مخاطب:** ایجنت مخزن `paymanshafayan/bazino-gamenet-portal`  
> **مرجع کد و طراحی قالب:** مخزن `paymanshafayan/bazino-arena-landing` — شاخه فعلی و پوشه `hub/`  
> **تاریخ:** 2026-09-08 (نسخه کامل نهایی: ۴ کارت Games، کاورهای 8K، پرداخت حضوری در کلاب)

---

```text
وظیفه شما: فعال‌سازی و راه‌اندازی پشتیبانی از قالب تمام‌نئونی «Hub» (شناسه: bazino-hub) در پورتال گیم‌نت بازینو به‌گونه‌ای که قالب به‌صورت بسته ZIP استاندارد از پنل مدیریت نصب و فعال شده و با داده‌های زنده و واقعی پورتال کار کند.

═══════════════════════════════════════════════════════════════════
۰) اطلاعات پایه و وضعیت فعلی پورتال
═══════════════════════════════════════════════════════════════════
۱. در حال حاضر پورتال شما دارای Theme SDK نسخه ۲ است (THEME_REGIONS در src/themeSdk/sdk.ts).
۲. متغیر layoutMode در پورتال عمداً روی 'classic' قفل شده بود تا طراحی قالب Hub تکمیل شود.
۳. طراحی کامل، تست‌شده و ۱۰۰٪ آماده‌ی قالب Hub در مخزن landing (پوشه hub/) با ۱۷ صفحه، دیزاین سیستم کامل، ۴ کارت صفحه Games، کاورهای باکیفیت 8K، درخت ۳۲ نفره مسابقات و پرداخت حضوری موجود است.
۴. قالب فعلی کلاسیک (bazino-arena v4.5.5) باید بدون تغییر و کاملاً بازگشت‌پذیر حفظ شود.

═══════════════════════════════════════════════════════════════════
۱) فاز A: تغییرات و ارتقای ساختار در سمت پورتال (Portal Engine)
═══════════════════════════════════════════════════════════════════

۱. رفع باگ پارسر ZIP در پورتال (EEXIST):
   - در فایل `src/themes/themeZipCore.ts`، متد باز کردن فایل زیپ، entryهای پوشه‌ای را مانند فایل پردازش می‌کند که در صورت وجود زیرپوشه (مانند `assets/`) خطا می‌دهد.
   - اصلاح: در حلقه خواندن فایل‌ها اضافه کنید: `if (raw.endsWith('/')) continue;`

۲. پشتیبانی از "layout": "hub" در Theme Engine:
   - در تایپ‌های ThemeInfo و theme.json، فیلد اختیاری `layout?: 'classic' | 'hub'` پشتیبانی شود.
   - زمانی که قالب فعال دارای `layout: "hub"` است:
     * حالت پیش‌فرض هدر و صفحات پورتال به رندرر قالب Hub تغییر مسیر می‌دهد.
     * صفحات `/games`, `/events`, `/events/*`, `/shop`, `/food`, `/club`, `/blog`, `/chat`, `/profile`, `/contact`, `/rules`, `/privacy` از کامپوننت‌های رجیسترشده‌ی Hub رندر می‌شوند.
     * صفحات `/legal/*`, `/payment/*`, درگاه PayTR پورتال و پنل مدیریت (`/admin`) دست‌نخورده باقی می‌مانند.

۳. ارسال Props واقعی پورتال به قالب Hub:
   - `user`, `onLogin`, `onLogout`: احراز هویت واقعی پورتال (OTP پیامکی + رمز عبور).
   - `tournaments`: لیست مسابقات هفتگی و رویدادهای ویژه همراه با تاریخ، ساعت، هزینه ورودی، جوایز ۱ تا ۳، ظرفیت و وضعیت.
   - `bracket`: اطلاعات زنده براکت ۳۲ نفره متصل به جریان SSE پورتال (بدون رفرش + نمایش Overlay تلویزیونی).
   - `systems`: تعرفه‌های ایستگاه‌های ۸۵ اینچ، ۶۵ اینچ، Kids و دسته‌های اضافی ۲۵ لیر/ساعت جهت محاسبه خودکار در صفحه Games.
   - `season`: اطلاعات لیدربورد فصل جاری، شمارش معکوس روزهای باقی‌مانده و امتیازات.
   - `settings`: نام کلاب، آدرس، ساعت کاری و شماره‌های تماس بدون هیچ داده‌ی هاردکد.
   - `language` و `dir`: پشتیبانی کامل از ۴ زبان (fa, en, tr, ru).

═══════════════════════════════════════════════════════════════════
۲) فاز B: ساخت بسته نصبی قالب Hub (bazino-hub-theme.zip)
═══════════════════════════════════════════════════════════════════

از روی سورس‌کدهای پوشه‌ی `hub/` در مخزن `paymanshafayan/bazino-arena-landing`:
۱. فایل `theme.js`:
   - منطق فرانت‌اند و کامپوننت‌های ۱۷ صفحه به ES5 خالص استاندارد تبدیل شود (سازگار با Theme SDK v2).
   - از توابع کمکی props برای ناوبری درون‌برنامه‌ای (SPA) استفاده شود.
۲. فایل `theme.css`:
   - ادغام توکن‌های نئونی (`tokens.css`, `neon-box.css`, `theme.css`) در استایل واحد ریشه `body[data-theme='bazino-hub']`.
   - فوتر چسبان (Sticky Footer) بدون ایجاد باند خالی زیر صفحه.
۳. پوشه `assets/`:
   - تمامی تصاویر و کاورهای باکیفیت 8K (`fc26.png`, `ufc5.png`, `mk1.png`, `tekken8.png`, `games-kids.jpg`, `games-adults.jpg`, `games-gear.jpg`, `games-requests.jpg`, `banner-bracket.jpg`, `slide-*.jpg`).
   - فونت‌های woff2 استاندارد (Orbitron و Rajdhani).
   - صفر درخواست به سرورهای خارجی (تمام منابع به‌صورت Local از پورتال لود شوند).
۴. رعایت اصول کلیدی طراحی:
   - صفحه Games دارای ۴ کارت کامل (KIDS / ADULTS / SYSTEMS & GEAR / GAME REQUESTS) به همراه فرم رزرو در محل.
   - بدون نقشه iframe (فقط کارت موقعیت با لینک مستقیم Google Maps).
   - پرداخت فقط به‌صورت حضوری در کلاب (`HOLD MY BAY`, `HOLD MY SEAT` - عدم اشاره به PayTR در UI قالب Hub).

═══════════════════════════════════════════════════════════════════
۳) معیارهای پذیرش و تست (QA Checklist)
═══════════════════════════════════════════════════════════════════
✓ تست نصب زیپ: دستور `npm run audit:theme -- bazino-hub-theme.zip` مقدار `canInstall: true` بدهد.
✓ نصب از پنل ادمین پورتال با موفقیت انجام شده و با فعال‌سازی آن کل پوسته به تم نئونی Hub تغییر کند.
✓ لاگین با OTP و خروج کامل کاربر بدون رفرش ناخواسته کار کند.
✓ تغییر وضعیت مسابقات در پنل ادمین به‌صورت زنده (SSE) روی درخت براکت ۳۲ نفره اعمال شود.
✓ بازگشت‌پذیری: با انتخاب مجدد قالب Classic یا پیش‌فرض، سایت بلافاصله به حالت اولیه بازگردد.
```

---

این فایل در مسیر ریشه ذخیره شده و می‌توانید متن داخل کادر بالا را مستقیماً به چت ایجنت پورتال ارسال کنید.
