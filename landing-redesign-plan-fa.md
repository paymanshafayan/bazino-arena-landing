# پلن طراحی هفت‌بخش Landing Page بازینو

## مبنای طراحی

این طراحی بر پایه‌ی زبان بصری **Hall of Legends** انجام می‌شود: زمینه‌ی زغالی/مشکی، طلایی قهرمانی، آبی الکتریکی، تایپوگرافی فشرده و حس یک شب مسابقه‌ای لوکس. محتوای صفحه کاملاً کنسولی می‌ماند و فقط PS5، Xbox Series X، سالن VIP، نمایشگرهای ۸۵ اینچی، Gaming Café، تورنومنت و اپلیکیشن بازینو را معرفی می‌کند. هیچ اشاره‌ای به PC یا سخت‌افزار PC اضافه نخواهد شد.

## هفت بخش اصلی

| شماره | بخش | نقش محتوایی | رفتار تعاملی |
|---:|---|---|---|
| ۱ | Hero / Mona Cinematic Stage | معرفی تجربه‌ی اصلی و شعار برند با ویدئوی یک‌بارپخش مونا | نشانگر عمقی ظریف، حرکت لایه‌های نور و اطلاعات صحنه با حرکت نشانگر؛ ویدئو بدون دکمه‌ی پایانی و بدون تکرار باقی می‌ماند. |
| ۲ | Console Arena / Loadout | معرفی PS5، Xbox Series X و تجربه‌ی نمایشگر ۸۵ اینچی | کارت‌ها با حرکت نشانگر عمق متفاوت می‌گیرند؛ Hover هر کارت، خط اسکن و برجسته‌شدن اطلاعات همان ایستگاه را فعال می‌کند. |
| ۳ | Active Tournaments | نمایش تورنومنت‌های فعال، وضعیت، تاریخ و اطلاعات رسمی جایزه | نوار وضعیت زنده، ورود ترتیبی آیتم‌ها و CTA به مسیر `/tournaments` و منبع رسمی اطلاعات. |
| ۴ | Match History / Scoreboard | نمایش نتایج مسابقات اخیر به‌صورت اسکوربورد سینمایی | ردیف‌ها هنگام اسکرول از دو سمت وارد می‌شوند؛ تغییر Hover روی نتیجه، برجسته‌سازی برنده و امتیاز را نشان می‌دهد. |
| ۵ | Lounge Services / VIP + Café | معرفی سالن VIP، Gaming Café، خدمات بین راندها و تجربه‌ی ۸۵ اینچی | تصویر/کارت‌های نامتقارن با لایه‌ی عمقی؛ CTAهای جداگانه به `/reservations` و `/cafe`. |
| ۶ | Passes + Entry Signal | معرفی تعرفه‌ها/پاس‌ها و مسیر سه‌مرحله‌ای ورود و رزرو | پاس انتخاب‌شده با Hover برجسته می‌شود؛ مراحل با اسکرول به‌ترتیب فعال می‌شوند؛ CTA اصلی به `/reservations`. |
| ۷ | Visit + Mobile Signal | آدرس Vistamare Hotel، مسیر، اپلیکیشن، اعلان‌های باشگاه و Footer | نقشه/مهر مکان با حرکت عمقی محدود؛ CTA مسیر، سایت رسمی و دانلود اپلیکیشن بدون ادعای لینک ساختگی. |

این هفت بخش، چهار سطح اجباری راهنما را پوشش می‌دهند: `gameGenres` در بخش ۲، `tournaments` در بخش ۳، `matchHistory` در بخش ۴ و `loungeSections` در بخش ۵. داده‌های اختیاری `featuredGames`، `pricingPackages` و `settings` در بخش‌های ۱ و ۶ و ۷ استفاده می‌شوند.

## سیستم تعامل با نشانگر

نشانگر روی دسکتاپ یک مختصات محدودشده‌ی صحنه تولید می‌کند و فقط transform و opacity لایه‌ها را تغییر می‌دهد تا عملکرد روان بماند. هر بخش یک عمق متفاوت دارد: پس‌زمینه کمترین حرکت، خطوط فنی حرکت متوسط و کارت فعال حرکت اندک اما محسوس. نشانگر نباید متن یا CTA را جابه‌جا کند و روی موبایل به لایه‌های ثابت و لمس‌پذیر تبدیل می‌شود.

## سیستم انیمیشن اسکرول

هر بخش با `IntersectionObserver` یا `whileInView` یک‌بار وارد می‌شود. ترتیب ورود: برچسب فصل، عنوان، توضیح، سپس کارت‌ها؛ فاصله‌ی تأخیر بین آیتم‌ها کوتاه و کنترل‌شده است. انیمیشن‌ها فقط transform و opacity را تغییر می‌دهند، با `prefers-reduced-motion` غیرفعال یا به ظهور فوری تبدیل می‌شوند، و هیچ بخش مهمی فقط به حرکت وابسته نخواهد بود.

## وفاداری به اینستاگرام

فرمت و لحن متن‌ها مانند کمپین بازینو باقی می‌ماند: شعارهای کوتاه، تمرکز بر رقابت، جایزه و تجربه‌ی محلی در İskele، CTA به سایت رسمی و اپلیکیشن، و زبان‌های ترکی، فارسی، انگلیسی و روسی. زبان پیش‌فرض ترکی می‌ماند. ظاهر کلی از قالب پست‌ها و ریلزها الهام می‌گیرد، اما با ساختار لوکس و سینمایی وب توسعه داده می‌شود. Mona فقط به‌عنوان میزبان تکرارشونده استفاده می‌شود و هویت تصویری قفل‌شده‌ی او تغییر نمی‌کند.

## دامنه‌ی اصلاحات

ابتدا کنترل‌های حذف‌شده‌ی Replay و Final CTA از Hero پاک می‌شوند. سپس Home.tsx و CSS اصلی بازآرایی می‌شوند و پکیج `theme-package` نیز با همان قرارداد سه‌فایلی و پوشش مسیرهای پورتال همگام می‌گردد. بعد از پیاده‌سازی، مسیرهای `/reservations`، `/cafe`، `/shop`، `/tournaments`، `/blog`، `/loyalty` و `/chat`، حالت موبایل، فوکوس صفحه‌کلید، کاهش حرکت و بیلد تولیدی بررسی خواهند شد.
