# یافته‌های PageSpeed Insights — bazino.pro

ثبت مرتب به‌ترتیب ارسال کاربر (شروع: ۲۰۲۶-۰۹-۰۴). برای هر مورد: مشخص می‌شود متعلق به
**قالب** (این ریپو — رفع می‌شود) یا **پورتال** (`bazino-gamenet-portal` — پرامپت/پچ تحویل داده می‌شود، خودمان دست نمی‌زنیم) است.

زمینه: تست روی نسخه‌ی لایو `bazino.pro` با قالب `bazino-arena v3.1.0` نصب‌شده (نشانه: `theme.js?v=1788473040631`).
تمرکز کاربر: بهینه‌سازی بیشتر برای **نمایش در گوشی (موبایل)**.

---

## بخش ۱ — طول عمر کش (Serve static assets with an efficient cache policy) — 🟡 پورتال

**داده‌ی PSI:**

| Request | Cache TTL | Transfer |
|---|---|---|
| `/api/themes/bazino-arena/assets/hero-arena.mp4` | 4h | 2,734 KiB |
| `/api/themes/bazino-arena/assets/hero-poster.webp` | 4h | 16 KiB |
| `/api/themes/bazino-arena/theme.js?v=1788473040631` | 4h | 9 KiB |
| `/logo.png` | 7d | 56 KiB |

**تحلیل:**
- هدرهای کش را سرور پورتال ست می‌کند؛ از داخل زیپ قالب قابل کنترل نیست → **غیرقابل رفع از سمت قالب**.
- کد پورتال: `theme.js` → `no-cache` (خط ~۲۹۸۳)؛ `assets/*` → `public, max-age=3600` (خط ~۳۰۰۵)؛ `theme.css` → `no-cache`.
- `theme.js` URLش از قبل نسخه‌دار است (`?v=<installedAt>` را `useThemeScript` می‌چسباند) → کش `immutable` یک‌ساله برایش ۱۰۰٪ امن است.
- `assets/*` نسخه‌دار نیست → برای کش بلند باید URL نسخه‌دار شود (`rewriteCssAssetUrls` + `assetsBase`) و بعد `immutable` بگیرد.
- PSI عدد 4h نشان می‌دهد که با `max-age=3600` کد نمی‌خواند → یک لایه‌ی کش روبه‌روی دامنه (CDN/پروکسی هاست) هم در کار است؛ باید تنظیم همان لایه هم بررسی شود.
- `/logo.png` متعلق به سایت مادر است (۷ روز، ۵۶KB) — اولویت پایین.

**اثر واقعی بر کاربر:** فقط بازدیدهای تکراری؛ ویدئو/پوستر بعد از `window load` بارگذاری می‌شوند و روی نمره‌ی LCP/FCP اثر مستقیم ندارند (PSI هم این موارد را «Unscored» علامت زده).

**اقدام قالب:** هیچ (خارج از کنترل قالب). **پرامپت تحویل‌شده به ایجنت پورتال:** پایین همین بخش.

<details>
<summary>پرامپت آماده برای ایجنت پورتال (کپی)</summary>

در `server.ts` بازینو، سیاست کش فایل‌های قالب نصب‌شده را اصلاح کنید:

1) `GET /api/themes/:id/theme.js` — الان `Cache-Control: no-cache` می‌فرستد در حالی که URL آن خودش نسخه‌دار است (`?v=<installedAt>` را `useThemeScript` می‌چسباند و هر نصب/جایگزینی مقدار تازه می‌سازد). پس بی‌خطر است با:
   `Cache-Control: public, max-age=31536000, immutable`
2) `GET /api/themes/:id/assets/*` — الان `public, max-age=3600` است. برای کش بلندِ امن، اول URLها را نسخه‌دار کنید و بعد immutable بدهید:
   - در `rewriteCssAssetUrls` (src/themes/themeZipCore.ts) هنگام بازنویسی `url('assets/…')` یک `?v=<installedAt یا هش محتوای فایل>` به URL اضافه شود.
   - `assetsBase` که به props قالب می‌رسد هم نسخه‌دار شود (یا سرور هنگام سرو، کوئری `v` را剥离 و اعتبارسنجی کند).
   - سپس: `Cache-Control: public, max-age=31536000, immutable`
   - اگر نسخه‌دار کردن الان نمی‌شود، حداقل: `public, max-age=86400`.
3) `theme.css` همان `no-cache` بماند (کوچک است و باید همیشه تازه باشد).
4) `/logo.png` (۷ روز) — اگر نام فایل هش/نسخه ندارد همین بماند؛ اولویت پایین.
5) PSI برای هر سه فایل قالب «4h» را گزارش می‌کند که با `max-age=3600` کد نمی‌خواند؛ لایه‌ی CDN/پروکسی جلوی دامنه را هم بررسی و هم‌راستا کنید.

</details>

**یادداشت جانبی (ثبت برای بخش‌های بعدی):** در پکیج قالب `assets/hero-poster-small.webp` (۶۴۰×۳۶۰، ~۷KB) موجود است ولی theme.js فعلاً پوستر ۱۲۸۰×۷۲۰ (~۱۵KB) را هم به موبایل می‌دهد — به‌روزرسانی جزئی سمت قالب برای انتخاب پوستر کوچک در نمایشگرهای باریک، در اولین بخش مرتبط (حجم/موبایل) انجام می‌شود.

---

## بخش ۲ — تحویل بهینه‌ی تصویر (Improve image delivery) — 🟢 سهم قالب رفع شد + سهم پورتال در پرامپت ۲

**داده‌ی PSI (صرفه‌جویی تخمینی ۳۰۱ KiB):**

| تصویر | اندازه | مشکل PSI |
|---|---|---|
| `moba-strategy-960.webp` (کارت ژانر) | 111.5 KiB | فایل 960×536 در نمایش 315px + فشرده‌سازی کم |
| `rpg-openworld-960.webp` (کارت ژانر) | 80.2 KiB | فایل بزرگ‌تر از نمایش |
| `logo.png` (لوگوی هرو قالب) | 54.4 KiB | PNG 256×256 در نمایش 42px؛ فرمت قدیمی |
| `sports-console-960.webp` (کارت ژانر) | 58.8 KiB | فایل بزرگ‌تر از نمایش |
| `esports-960.webp` (کارت ژانر) | 44.2 KiB | فایل بزرگ‌تر از نمایش |

**تفکیک مسئولیت:**
- المان‌های `<img>` توسط theme.js قالب رندر می‌شوند (`bazino-home-card-media` و `theme-brand-logo`) —
  اما **فایل‌ها و URLها از داده‌ی پورتال** می‌آیند (`gameGenres[].imageUrl` و `/logo.png`).
- فایل‌های واریانت `-320/-480/-640` از قبل روی دیسک پورتال موجودند و UI پیش‌فرض خود پورتال هم
  برای همان داده‌ها `getResponsiveSrcSet([320,480,640])` می‌سازد؛ ولی داده‌ی داده‌شده به قالب فقط `-960` است.

**✅ اقدام قالب (نسخه ۳.۲.۰):**
1. تصاویر کارت‌ها (ژانر/تورنمنت/سالن) و اسلایدر سالن حالا **srcSet + sizes** می‌گیرند: اگر URL با
   الگوی پورتال (`/images/…-<width>.webp`، با حفظ مهر `?v=`) بخواند، واریانت‌های 320/480/640 + اصل
   فایل عرضه می‌شوند → گوشی کوچک‌ترین فایل لازم را می‌گیرد (پوشش هر ۴ تصویر کارت بالایی).
2. fallback امن: `src` همیشه URL اصلی است؛ اگر واریانتی 404 شد `onError` یک‌باره به اصل برمی‌گردد.
3. URLهای بی‌الگو (مثلاً `/uploads/…`) هیچ srcSetی نمی‌گیرند (تست شد).
4. **پوستر هرو موبایل**: نمایشگر ≤800px پوستر 640×360 (~7KB) می‌گیرد به‌جای 1280×720 (~15KB)؛
   کلاس `is-small-screen` پس‌زمینه‌ی CSS را هم هم‌آدرس می‌کند تا فقط یک دانلود انجام شود.
5. تأییدها: syntax/ES5، رندر SSR (ساخت srcSet دقیق با حفظ `?v=`، sizes کارت `(min-width:801px) 25vw,100vw`
   و سالن `1120px`)، ممیزی نصب پورتال `canInstall: true`.

**⛔ باقی‌مانده برای پورتال (پرامپت ۲):** لوگوی PNG (۵۴KB، مهم‌ترین مورد چون بالای صفحه است)،
اضمینان کامل بودن ست واریانت‌ها، فشرده‌سازی دوباره‌ی `moba-strategy-960`، و در بلندمدت فیلد
`imageUrlSmall` در قرارداد داده.

**تست‌نشده:** اندازه‌گیری مجدد PSI پس از دپلوی (و UID واقعی دانلود واریانت‌ها در گوشی).

---

## بخش ۳ — عوامل شیفت چیدمان (Layout shift culprits / CLS) — 🟢 سهم قالب رفع شد + سهم پورتال در پرامپت ۳

**داده‌ی PSI (مجموع 0.157):**

| عنصر | شیفت |
|---|---|
| `<main class="flex-grow z-10 w-full relative">` | 0.078 (دو بار) |
| `<div dir="ltr" class="bazino-hero-content theme-frame">` (قالب) | 0.048 |

**تحلیل:**
- **0.048 — سمت قالب (رفع شد):** پورتال اسلایدهای ادمین را `setTimeout` ۲.۵ ثانیه‌ای بعد از mount می‌فرستد؛ تیتر هرو از اسلایدهای نمونه به تیتر ادمین عوض می‌شد و چون تعداد خطوط h1 تغییر می‌کرد، CTA و دات‌ها می‌پریدند (چرخش هر ~۷.۵ ثانیه هم همین‌طور، به‌طور تکرارشونده).
  ✅ رفع در ۳.۳.۰: `.bazino-hero-slide` min-height رزروی گرفت (دسکتاپ ~۲ خط تیتر + زیرتیتر؛ موبایل بیشتر) → تعویض تیترهای معمولی دیگر شیفت نمی‌سازد. تیترهای خیلی بلند هنوز ممکن است بلوک را کمی قد بکشند (باقی‌مانده‌ی آگاهانه).
- **0.078×۲ — سمت پورتال (پرامپت ۳):**
  1. `ThemeRegion name="header"` بدون `pending` است → تا لود theme.js **هدر هیچ رندر نمی‌شود**، بعد یک‌باره ظاهر می‌شود و کل `main` را به پایین هل می‌دهد (بزرگ‌ترین شیفت).
  2. جایگزینی placeholder هوم: `min-h-[600px]` (App.tsx:562 و HomeTab.tsx:643) با هوم واقعی قالب (چند هزار پیکسل) → فوتر و هر چه زیر است جابه‌جا می‌شود. شیift عناصر خارج از viewport امتیاز ندارد؛ اگر placeholder حداقل یک صفحه بود (min-h-screen)، این شیفت عملاً حذف می‌شد.
  3. theme.css قالب‌های سروری بعد از paint با JS تزریق می‌شود → استایل نهایی دیر می‌رسد.
  4. فونت‌های محلی (index.css) با font-display معمولی بعد از paint سواپ می‌شوند → متریک تیترها تغییر می‌کند.
  5. اسلایدها با تأخیر ۲.۵ ثانیه‌ای می‌رسند (حالا قالب فضا رزرو کرده، اما داده‌ی زودتر = شیفت کمتر).

**تست‌نشده:** اندازه‌گیری مجدد CLS با PSI پس از دپلوی هر دو طرف.

---

## بخش ۴ — کشف‌پذیری درخواست LCP (LCP request discovery) — 🟢 سهم قالب رفع شد + سهم پورتال در پرامپت ۴

**داده‌ی PSI:** عنصر LCP: `div.bazino-home > section.bazino-chapter > div.bazino-hero-media > div.bazino-hero-poster-layer`
- LCP resources should not use loading=lazy
- fetchpriority=high should be applied
- Request is discoverable in initial document

**تحلیل:** پوستر LCP قالب به‌صورت **background-image در CSS** رندر می‌شد → مرورگر آن را فقط بعد از ساخت
CSSOM و match شدن عنصر کشف می‌کند؛ background هیچ‌وقت `fetchpriority` نمی‌گیرد و در preload scanner هم دیده نمی‌شود.

**✅ اقدام قالب (نسخه ۳.۴.۰):**
1. پوستر حالا **`<img>` واقعی** است (اولین فرزند `.bazino-hero-media`) با `fetchPriority="high"`،
   `decoding="async"` و بدون هیچ اتریبیوت `loading` (یعنی eager، نه lazy) — سه بولتن PSI مستقیماً آدرس شدند.
2. CSS دیگر هیچ تصویری لود نمی‌کند — فقط چیدمان (absolute/inset/object-fit)؛ قانون background-image و
   override موبایل حذف شدند (انتخاب URL پوستر کوچک از قبل در JS است — ۳.۲.۰).
3. همان URL در `poster` ویدئو هم هست → همچنان یک دانلود مشترک؛ `onError` پوستر را در صورت خطا می‌پوشاند.
4. تأییدها: رندر SSR (تگ دقیق با fetchpriority=high، بدون lazy، src درست، اولین لایه) + `canInstall: true`.

**⛔ باقی‌مانده سمت پورتال (پرامپت ۴):** «قابل کشف در سند اولیه» کامل نمی‌شود مگر اینکه URL پوستر در HTML
اولیه باشد — سایت SPA است و همه‌چیز بعد از JS رندر می‌شود. راه‌حل پیشنهادی: تزریق
`<link rel="preload" as="image" fetchpriority="high">` برای `theme.json.media.heroPoster` قالب فعال
در head همان HTML سرو‌شده (به‌همراه در نظر گرفتن واریانت موبایل `heroPosterSmall`).

**تست‌نشده:** اندازه‌گیری مجدد LCP با PSI پس از دپلوی.

---

*(بخش‌های بعدی در ادامه ثبت می‌شوند)*
