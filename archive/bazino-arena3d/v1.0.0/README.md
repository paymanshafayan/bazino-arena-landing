# Bazino Arena 3D v1.0.0 archive

Immutable source snapshot of the previous theme version before the v1.0.1 preview rebuild.
The original installable ZIP remains available in repository history at commit `1a06856^`.
Do not use this directory as the active package; active source is `bazino-arena3d-package/`.
