# اعتبارسنجی قابلیت‌های جدید Home

در نسخه‌ی اصلی، اسلایدر VIP/Café هر ۵٫۲ ثانیه تصویر را تغییر می‌دهد و هنگام ورود نشانگر موس یا Focus کیبورد روی سطح اسلایدر متوقف می‌شود. با خروج از سطح، Autoplay دوباره فعال می‌شود و کنترل‌های قبلی/بعدی همچنان قابل استفاده هستند.

بخش تورنومنت اکنون دارای جستجوی زنده، فیلتر ژانر و انتخاب مرتب‌سازی بر اساس حالت پیشنهادی، تاریخ یا مبلغ جایزه است. مرتب‌سازی فقط در صورت وجود فیلدهای رسمی `publishedAt` یا `prizeValue` اعمال می‌شود و داده‌ی جعلی برای تاریخ یا مبلغ تولید نشده است.

Loader لوگومحور پیش از نمایش کامل Home ظاهر می‌شود، پس از آماده‌شدن فونت و سپری‌شدن زمان کوتاه حذف می‌شود، و برای `prefers-reduced-motion` حرکت‌های تزئینی را کاهش می‌دهد.

TypeScript، بیلد تولیدی، رندر دسکتاپ/موبایل و ZIP پکیج تم بررسی شدند. نسخه‌ی نصب‌پذیر theme.js و theme.css نیز با این قابلیت‌ها همگام شده‌اند.
